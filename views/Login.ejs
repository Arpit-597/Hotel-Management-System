<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sign In</title>
    <link href="https://fonts.googleapis.com/css?family=Poppins:100,200,300,400,500,600,700,800,900&display=swap"
        rel="stylesheet">
    <!-- Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Poppins", sans-serif;
        }

        #reg_login {
            position: relative;
            min-height: 100vh;
            background-image: url(https://res.cloudinary.com/diyp1k5z5/image/upload/v1744874374/bg_v79sxr.jpg);
            background-size: cover;
            background-repeat: no-repeat;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .container {
            position: relative;
            width: 800px;
            height: 500px;
            background: #fff;
            box-shadow: 0 15px 50px rgba(0, 0, 0, 0.1);
            display: flex;
        }

        .imgBx {
            width: 50%;
            height: 100%;
        }

        .imgBx img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transform: translateX(-12px);
        }

        .formBx {
            width: 50%;
            height: 100%;
            background: #fff;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 40px;
        }

        .formBx form {
            width: 100%;
        }

        .formBx h2 {
            font-size: 18px;
            font-weight: 600;
            text-transform: uppercase;
            text-align: center;
            margin-bottom: 10px;
            color: #555;
        }

        .formBx input {
            width: 100%;
            padding: 10px;
            background: #f5f5f5;
            color: #333;
            border: none;
            margin: 8px 0;
            font-size: 14px;
        }

        .formBx input:hover {
            border: 1px solid #62260dab;
        }

        .formBx input[type="submit"] {
            max-width: 100px;
            background: #62260dab;
            color: #fff;
            cursor: pointer;
            font-size: 14px;
            font-weight: 500;
            padding: 10px;
            margin-top: 5px;
        }

        .hi {
            max-width: 100px;
            background: #62260dab;
            color: #fff;
            cursor: pointer;
            font-size: 14px;
            font-weight: 500;
            letter-spacing: 1px;
            padding: 10px;
            border: none;
            outline: none;
            box-shadow: none;
            margin-top: 5px;
        }

        .formBx input[type="submit"]:hover {
            background: #62260d;
        }

        .signup {
            margin-top: 20px;
            font-size: 12px;
            color: #555;
            text-transform: uppercase;
            text-align: center;
        }

        @media (max-width: 991px) {
            .container {
                max-width: 400px;
                flex-direction: column;
                height: auto;
            }

            .imgBx {
                display: none;
            }

            .formBx {
                width: 100%;
                height: auto;
            }
        }

        .hii {
            margin-top: 20px;
            font-size: 12px;
            letter-spacing: 1px;
            color: #555;
            text-transform: uppercase;
        }

        .hiii {
            font-weight: 600;
            text-decoration: none;
            color: #62260d;
            cursor: pointer;
        }
    </style>
</head>

<body>

    <section id="reg_login">
        <div class="container">
            <div class="imgBx">
                <img src="https://res.cloudinary.com/diyp1k5z5/image/upload/v1744874379/login-img_cdeenv.jpg"
                    alt="Login Image" />
            </div>
            <div class="formBx">
                <form action="/verifyLogin" method="post">
                    <h2>Sign In</h2>
                    <center>
                        <% if(success.length>0){%>
                            <div class="alert alert-success">
                                <%= success %>
                            </div>
                            <%}%>
                                <% if(error.length>0){%>
                                    <div class="alert alert-danger">
                                        <%= error %>
                                    </div>
                                    <%}%>
                    </center>
                    <input type="email" name="email" placeholder="Email" required />
                    <input type="password" name="password" placeholder="Password" required />
                    <button type="submit" class="hi">Sign In</button>
                    <p class="hii signup">Don't have an account? <a href="/signup" class="hiii">Sign Up</a></p>
                </form>
            </div>
        </div>
    </section>

</body>

</html>